import React from 'react';
import ReactDOM from 'react-dom/client';
import ZellijApp from './components/ZellijApp.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ZellijApp />
  </React.StrictMode>
);
